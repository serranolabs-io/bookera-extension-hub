import { css } from 'lit';
export default css`
  .input-box {
    display: flex;
    gap: var(--spacingSmall);
    flex-direction: vertical;
  }

  .horizontal {
    display: flex;
    gap: var(--spacingSmall);
  }
  .red {
    color: var(--sl-color-red-500);
  }
`;
